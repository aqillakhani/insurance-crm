/**
 * Forms page — ACORD forms and insurance documents
 * Reference: Nav item visible in screenshots
 */
export default function FormsPage() {
  return (
    <div className="p-3">
      <h2 style={{ fontSize: "16px", fontWeight: "bold", color: "var(--text)", marginBottom: "12px" }}>
        Forms
      </h2>
      <div className="p-4 text-center" style={{ background: "var(--bg)", border: "1px solid var(--border)", color: "var(--text-muted)", fontSize: "13px" }}>
        ACORD forms and insurance document templates will be available here.
      </div>
    </div>
  );
}
